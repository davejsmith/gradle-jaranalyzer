
apply plugin:'groovy'
apply plugin:'idea'

repositories {
    maven { url 'http://repos.ssdt.nwoca.org/artifactory/list/ext-releases-local/' }
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile(group: 'com.kirkk', name: 'jaranalyzer', version: '1.2')
}
